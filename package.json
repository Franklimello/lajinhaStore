{
  "name": "ecoomerce",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@capacitor/android": "^7.4.4",
    "@capacitor/browser": "^7.0.2",
    "@capacitor/cli": "^7.4.4",
    "@capacitor/core": "^7.4.4",
    "@codetrix-studio/capacitor-google-auth": "^3.4.0-rc.4",
    "ajv": "^8.17.1",
    "firebase": "^12.0.0",
    "localforage": "^1.10.0",
    "qrcode": "^1.5.4",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "react-helmet-async": "^2.0.5",
    "react-icons": "^5.5.0",
    "react-router-dom": "^7.7.1",
    "react-scripts": "5.0.1",
    "socket.io-client": "^4.8.1",
    "swiper": "^11.2.10",
    "uuid": "^13.0.0"
  },
  "devDependencies": {
    "@google-cloud/storage": "^7.17.2",
    "autoprefixer": "^10.4.21",
    "postcss": "^8.5.6",
    "tailwindcss": "^3.4.17",
    "typescript": "^5.9.3"
  },
  "scripts": {
    "setup": "node scripts/setup-wizard.js",
    "validate-config": "node scripts/validate-config.js",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "build:optimized": "npm run optimize-images && npm run build",
    "optimize-images": "node scripts/optimize-images.js",
    "optimize-and-upload": "node scripts/optimize-and-upload.js",
    "upload-images": "node scripts/optimize-and-upload.js",
    "test-optimization": "node scripts/test-optimization.js",
    "firebase-upload": "node scripts/optimize-and-upload.js",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "cap:sync": "npx cap sync",
    "cap:copy": "npx cap copy",
    "cap:android": "npx cap open android",
    "cap:ios": "npx cap open ios",
    "cap:build": "npm run build && npx cap copy",
    "cap:sync:android": "npm run build && npx cap sync && npx cap open android",
    "cap:sync:ios": "npm run build && npx cap sync && npx cap open ios",
    "deploy:web": "npm run build && firebase deploy --only hosting",
    "deploy:android": "npm run build && npm run cap:copy && cd android && .\\gradlew.bat bundleRelease",
    "deploy:all": "npm run build && firebase deploy --only hosting && npm run cap:copy && cd android && .\\gradlew.bat bundleRelease"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
